name: platform_data
description: Flutter specified platform data, A cleaner way to know users platform type.
version: 0.0.3
homepage: https://github.com/benyaminbeyzaie/platform_data

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: